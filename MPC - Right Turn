#include <kipr/wombat.h>

int main()
{ 
    cmpc(0);//clear the motor position counter
    
    while (gmpc(0)<=1115)  //drive forward until it hits this # on mpc.  It will go a little further, may have to adjust
    {
   	motor (0,50);
    	motor (3,-50);
        }
    ao();

       return 0;
}
